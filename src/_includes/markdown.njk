{% extends 'home.njk' %}
{% block body %}
<!-- Header with Image -->
<header class="relative w-full py-40 bg-gray-200">
  <div class="absolute inset-0 h-full">
    <img class="object-cover w-full h-full" src="{{image}}" alt="{{image_alt}}">
    <div class="absolute inset-0 bg-gray-400 mix-blend-multiply" aria-hidden="true"></div>
  </div>
  <div class="relative px-4 py-24 mx-auto max-w-7xl sm:py-32 sm:px-6 lg:px-8">
    <p class="text-base font-semibold tracking-wider text-center uppercase text-sky-200">{{ category }}</p>
    <h1 class="text-4xl font-extrabold tracking-tight text-gray-100 sm:text-5xl lg:text-6xl">{{title}}</h1>
    <p class="max-w-3xl mt-6 text-xl text-indigo-100">{{description}}</p>
  </div>
</header>

  <article
    class="container px-4 py-8 mx-auto prose sm:px-6 lg:px-8 lg:prose-xl prose-code:bg-gray-100">

    <!-- header -->
    {# <header class="mx-auto text-lg max-w-prose">
      <p class="text-base font-semibold tracking-wider text-center uppercase text-sky-600">{{ category }}</p>
      <h1>
        <span class="block mt-2 text-3xl font-extrabold leading-8 tracking-tight text-center text-gray-900 sm:text-4xl">{{ title }}</span>
      </h1>
      <p class="mt-8 text-xl leading-8 text-gray-500">{{ description }}</p>
    </header> #}

    <!-- image -->
    {# <figure>
      <img class="w-full" src="{{ image }}" alt="{{ image_alt }}" width="1600" height="900">
    </figure> #}
    <div class="text-gray-800">
      {{ content | safe }}
    </div>
  </article>
{% endblock %}`